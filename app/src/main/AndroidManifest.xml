<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.doubletapp.hermitage.hermitage">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.TabActivity" android:theme="@style/NoTitleTheme" />
        <activity android:name=".ui.ticket.BuyTicketActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.home.HomeItemActivity" />
        <activity android:name=".ui.nav.NavItemActivity" android:theme="@style/NavDetailsTheme"/>
        <activity android:name=".ui.exhibit.ExhibitActivity" android:theme="@style/NavDetailsTheme"/>
    </application>

</manifest>